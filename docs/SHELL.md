# 已废弃：WebView2 桌面壳

`luma-shell`、tao/wry 无边框窗口和 WebView2 产品路径已于 2026-09-22 移除。

当前运行方式是 `luma-engine` 同进程 Windows 托盘 + 系统默认浏览器。请阅读
[TRAY.md](TRAY.md)。`screenshots/luma-shell-connecting.png` 仅作为历史设计记录保留，
不代表当前产品入口。
