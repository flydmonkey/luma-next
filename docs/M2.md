# M2 雏形：最小录制控制面

引擎根页面现已直接调用真实 session API：

- 空闲时显示“开始录制”。
- 录制时每秒刷新 elapsed，并提供“停止并验证”。
- 停止成功后显示输出路径、媒体时长、编码帧数和文件大小。
- API 或 ffprobe 验证失败时显示原始可操作错误，不显示假成功。

该页面被 `luma-shell` 原样托管；未引入第二套后端。外观基准见
[M2 WebUI 截图](screenshots/luma-webui-m2.png)。

本阶段不包含 M3 资料库、删除、设置持久化，也不包含 M4 编码器选择。
