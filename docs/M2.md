# M2：正式录制控制面

引擎根页面现已直接调用真实 session API：

- 空闲时显示“开始录制”。
- 录制时每秒刷新 elapsed，并提供“停止并验证”。
- 停止成功后显示输出路径、媒体时长、编码帧数和文件大小。
- API 或 ffprobe 验证失败时显示原始可操作错误，不显示假成功。

M3 批次已将测试页替换为 `web/` 中的正式页面；原页保留在 `/dev` 供诊断。正式页面
沿用旧 Luma 的状态、模式、快捷设置和单一主操作顺序，由引擎托管并在系统默认浏览器
打开，未引入第二套后端。

片库与设置现由 M3 提供；M4 编码器选择仍未开始。
